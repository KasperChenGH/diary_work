# Secrets — never commit actual keys
.env

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# External framework (cloned repo, not our code)
superpowers/

# npm dependencies (installed at runtime via npx)
node_modules/
